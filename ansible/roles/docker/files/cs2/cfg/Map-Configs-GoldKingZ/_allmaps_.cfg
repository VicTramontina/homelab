// Runs first on every map (Map-Configs-GoldKingZ). Resets to vanilla and
// unloads the mode plugins; the prefix cfgs next to this file (bhop_, surf_,
// arena_) then apply their mode and load what they need.
gkz_exec "homelab/modes/default.cfg"
gkz_unload SharpTimer
gkz_unload K4-Arenas
