[gameserver]
# mDNS is installed (avahi-daemon) -- this stays valid across router/DHCP
# changes, unlike a hardcoded IP.
gameserver-pc ansible_host=bigaserver.local ansible_user=victor_tramontina

[gameserver:vars]
ansible_python_interpreter=/usr/bin/python3
