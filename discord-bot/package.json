{
  "name": "homelab-discord-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker implementing the homelab Discord bot (HTTP interactions).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "register-commands": "node scripts/register-commands.js"
  },
  "dependencies": {
    "discord-interactions": "^4.4.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
